#!/usr/local/bin/perl

use Bayesian;
use Cache;
use CGAPConfig;
use CGI;
use CGI::Carp qw(fatalsToBrowser);
use DBI;
use DB_SCHEMAConfig;
use DKView;
use Exporter ();
use GD;
## use GENOMICSDKViewBK;
use Getopt::Long;
use GetPvalueForT;
use Paging;
use SAGE;
use Scan;
use strict;

print "It is OK.";
